import { ContentLibrary } from "@/components/crm/ContentLibrary";

export const metadata = { title: "Content Library — Venue OS" };

export default function ContentPage() {
  return <ContentLibrary />;
}
